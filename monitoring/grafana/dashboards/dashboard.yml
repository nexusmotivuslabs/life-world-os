apiVersion: 1

providers:
  - name: 'Local Build with Dev'
    orgId: 1
    folder: 'Local Build with Dev'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/local
      foldersFromFilesStructure: true

