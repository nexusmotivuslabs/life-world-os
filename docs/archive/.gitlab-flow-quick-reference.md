# GitLab Flow Quick Reference

**Status**: ✅ Active  
**Last Updated**: 2025-01-15

---

## Branch Structure

```
main (production)
  └── staging (staging)
      └── feature/* (features)
```

---

## Common Workflows

### Start Feature
```bash
git checkout main && git pull
git checkout -b feature/your-feature-name
```

### Sync with Staging
```bash
git checkout feature/your-feature
git pull origin staging
```

### Merge to Staging
```bash
git checkout staging && git pull
git merge feature/your-feature
git push origin staging
```

### Deploy Staging
```bash
npm run staging:deploy
```

### Promote to Production
```bash
git checkout main && git pull
git merge staging
git tag -a v1.0.0 -m "Release v1.0.0"
git push origin main --tags
npm run prod:deploy
```

---

## Commit Message Format

```
<type>(<scope>): <subject>

<body>
```

**Types**: `feat`, `fix`, `docs`, `refactor`, `test`, `chore`

**Example**:
```
feat(blog): add blog dropdown to header

Adds dropdown menu in header that lists blog posts.
Clicking opens post in modal with markdown rendering.
```

---

## Branch Naming

- Features: `feature/description`
- Hotfixes: `hotfix/description`

---

## Full Documentation

- [GitLab Flow Guide](./docs/confluence/domains/platform-engineering/implementation/gitlab-flow-guide.md)
- [Best Practices](./docs/confluence/domains/platform-engineering/implementation/gitlab-flow-best-practices.md)

---

**Quick Reference Only** - See full guides for details

